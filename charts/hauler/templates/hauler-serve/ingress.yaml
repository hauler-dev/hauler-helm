{{- if or (and .Values.haulerRegistry.enabled .Values.haulerRegistry.ingress.enabled) (and .Values.haulerFileserver.enabled .Values.haulerFileserver.ingress.enabled) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hauler-registry
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hauler.labels" . | nindent 4 -}}
    {{- with .Values.hauler.labels -}}
{{ toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.haulerRegistry.ingress.labels -}}
{{ toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.haulerFileserver.ingress.labels -}}
{{ toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- with .Values.hauler.annotations -}}
{{ toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.haulerRegistry.ingress.annotations -}}
{{ toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.haulerFileserver.ingress.annotations -}}
{{ toYaml . | nindent 4 }}
    {{- end }}
spec:
  rules:
  {{- if and .Values.haulerRegistry.enabled .Values.haulerRegistry.ingress.enabled }}
  - host: {{ .Values.haulerRegistry.ingress.hostname }}
    http:
      paths:
      - backend:
          service:
            name: hauler-registry
            port:
              number: {{ .Values.haulerRegistry.service.ports.targetPort }}
        path: /
        pathType: Prefix
  {{- end }}
  {{- if and .Values.haulerFileserver.enabled .Values.haulerFileserver.ingress.enabled }}
  - host: {{ .Values.haulerFileserver.ingress.hostname }}
    http:
      paths:
      - backend:
          service:
            name: hauler-fileserver
            port:
              number: {{ .Values.haulerFileserver.service.ports.targetPort }}
        path: /
        pathType: Prefix
  {{- end }}
  tls:
  {{- if and .Values.haulerRegistry.ingress.enabled .Values.haulerRegistry.ingress.tls.enabled }}
  - hosts:
    - {{ .Values.haulerRegistry.ingress.hostname }}
    secretName: {{ .Values.haulerRegistry.ingress.tls.secretName }}
  {{- end }}
  {{- if and .Values.haulerFileserver.ingress.enabled .Values.haulerFileserver.ingress.tls.enabled }}
  - hosts:
    - {{ .Values.haulerFileserver.ingress.hostname }}
    secretName: {{ .Values.haulerFileserver.ingress.tls.secretName }}
  {{- end }}
{{- end }}